import React from 'react';export default function RegisterTable({ classeId, classeData, backend, socket }){ const students = classeData?.students || []; const markPresence = async (sId)=>{ await fetch(backend+`/api/classes/${classeId}/presence`, { method:'POST', headers:{'Content-Type':'application/json'}, body:JSON.stringify({ studentId:sId, date:new Date().toISOString().slice(0,10), hour:1, status:'P', byUser:'prof1@registro.it' }) }); }; return (<div className='card'><h3>Registro {classeId}</h3><table><thead><tr><th>Studente</th><th>Presenza</th></tr></thead><tbody>{students.map(s=> (<tr key={s.id}><td>{s.name}</td><td><button className='btn' onClick={()=>markPresence(s.id)}>Segna Presenza</button></td></tr>))}</tbody></table></div>);}