import React, { useState } from 'react';export default function Login({ onLogin, backend }){const [id, setId] = useState('');const [pw, setPw] = useState('');const doLogin = async ()=>{try{const res = await fetch(backend+'/api/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({id,password:pw})});const j=await res.json();if(!res.ok) return alert(j.error||'Credenziali errate');onLogin(j);}catch(e){alert('Errore di connessione');}};return (<div style={{minHeight:'72vh',display:'flex',alignItems:'center',justifyContent:'center'}}><div style={{width:380}} className='card'><h2>RegistroSimulazione</h2><div className='small'>Accesso professori e studenti</div><div style={{marginTop:12}}><label className='small'>Codice</label><input className='input' value={id} onChange={e=>setId(e.target.value)} placeholder='es. prof1@registro.it' /><label className='small' style={{marginTop:8}}>Password</label><input type='password' className='input' value={pw} onChange={e=>setPw(e.target.value)} placeholder='password' /><button className='btn' style={{marginTop:12}} onClick={doLogin}>Accedi</button><div style={{marginTop:8}} className='small'>Prova: prof1@registro.it / 1234 — stud1A@registro.it / 1111</div></div></div></div>);}