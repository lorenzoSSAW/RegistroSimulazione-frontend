import React from 'react';export default function StudentView({ user, classes, onLogout }){ const cls = classes || { students: [] }; return (<div className='app'><div className='header card'><div><strong>{user.name}</strong><div className='small'>Studente — {user.classId}</div></div><div><button className='btn' onClick={onLogout}>Logout</button></div></div><div style={{marginTop:12}} className='card'><h3>Le tue informazioni</h3><p>Media stimata e presenze personali saranno qui.</p></div></div>);}